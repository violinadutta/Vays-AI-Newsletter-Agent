# Development & CI dependencies. Never installed on a handover/production machine.
-r requirements.txt

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0
pytest-mock>=3.14,<4.0
respx>=0.21,<1.0                # mock httpx: simulate 429/500/timeout without a real endpoint
hypothesis>=6.100,<7.0          # property-based tests for the text cleaner

# ── Quality gates ────────────────────────────────────────────────────────────
ruff>=0.5,<1.0                  # lint + format
mypy>=1.10,<2.0
import-linter>=2.0,<3.0         # enforces the layer rule (TRD §1.2) mechanically

# ── Security ─────────────────────────────────────────────────────────────────
pip-audit>=2.7,<3.0             # known-vulnerability scan
detect-secrets>=1.5,<2.0        # pre-commit hook: catches a committed API key

# ── Type stubs ───────────────────────────────────────────────────────────────
types-beautifulsoup4
types-PyYAML
pandas-stubs
